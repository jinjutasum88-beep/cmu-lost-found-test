name: ตั้งสิทธิ์แอดมิน

on:
  workflow_dispatch:
    inputs:
      email:
        description: "อีเมลแอดมิน"
        required: true
        type: string

jobs:
  set-admin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - run: npm install --omit=dev

      - name: ตั้งสิทธิ์แอดมิน
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node scripts/set-admin.mjs "${{ inputs.email }}"
