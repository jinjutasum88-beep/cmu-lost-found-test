# CMU LOST&FOUND — คู่มือติดตั้งและใช้งานฉบับสมบูรณ์

ระบบของหาย–ของพบ มหาวิทยาลัยเชียงใหม่
หน้าเว็บ + ฐานข้อมูล Firebase + ตัวจับคู่ NLP ฝั่งเซิร์ฟเวอร์ + ระบบส่งอีเมลแจ้งเตือนอัตโนมัติ
ทั้งหมดใช้บริการฟรี ไม่ต้องผูกบัตรเครดิต

---

## โครงสร้างไฟล์

```
cmu-lost-found-final/
├── index.html                      หน้าเว็บทั้งหมด (auth + แอป)
├── verify.html                     หน้ารับลิงก์ยืนยันอีเมล/ตั้งรหัสผ่านใหม่
├── firebase-config.js              ค่าตั้งต้น แก้ที่นี่ที่เดียว
├── password-policy.js              กฎและตัววัดความแข็งแรงของรหัสผ่าน
├── i18n.js                         ระบบสองภาษา ไทย/English
├── evaluation-dataset.mjs          ชุดข้อมูลติดฉลาก 20 คู่ สำหรับวัด precision/recall
├── matching.js                     เครื่องมือจับคู่ด้วย NLP (ใช้ร่วมกับเซิร์ฟเวอร์)
├── firestore.rules                 กติกาความปลอดภัยของฐานข้อมูล
├── package.json                    dependency ของสคริปต์ฝั่งเซิร์ฟเวอร์
├── scripts/
│   ├── match-and-notify.mjs        ตัวจับคู่ + ส่งอีเมล (รันบน GitHub Actions)
│   ├── set-admin.mjs               ตั้งสิทธิ์แอดมินด้วย custom claim
│   └── evaluate.mjs                รายงานผล precision/recall แบบละเอียด
├── tests/
│   ├── matching.test.mjs           ทดสอบตรรกะการจับคู่
│   ├── auth.test.mjs               ทดสอบกฎรหัสผ่านและการตรวจอีเมล
│   └── evaluation.test.mjs         ทดสอบว่าความแม่นยำยังผ่านเกณฑ์ SMART Goal
└── .github/workflows/
    ├── match-and-notify.yml        ตั้งเวลาให้รันทุก 5 นาที
    └── tests.yml                   รันชุดทดสอบทุกครั้งที่ push
```

---

## สถาปัตยกรรมโดยรวม

```
ผู้ใช้ ──lงประกาศ──► Firestore (posts, processed=false)
                          │
                          ▼
        GitHub Actions (ทุก 5 นาที, ใช้ Admin SDK)
        ├─ อ่านประกาศที่ยังไม่ประมวลผล
        ├─ วิเคราะห์ด้วย NLP (matching.js)
        ├─ เขียนผลลง matches พร้อม "สำเนาย่อ" ของอีกฝั่ง
        └─ ส่งอีเมลผ่าน Brevo API
                          │
                          ▼
     ผู้ใช้เปิดเว็บ กดยืนยัน ✓/✕ (อัปเดต matchStatus)
                          │
                          ▼
        GitHub Actions รอบถัดไป เห็นสถานะ accepted
        └─ เขียนข้อมูลติดต่อลงเอกสาร + ส่งอีเมลให้ทั้งสองฝ่าย
```

**จุดสำคัญด้านความเป็นส่วนตัว:** ผู้ใช้อ่านได้เฉพาะประกาศของตัวเองเท่านั้น
ไม่มีหน้ากระดานรวมที่เห็นประกาศคนอื่น รายละเอียดของอีกฝั่งจะเห็นได้ก็ต่อเมื่อระบบจับคู่ให้แล้ว
และอีเมลของอีกฝ่ายจะเปิดเผยหลังกดยืนยันว่าใช่เท่านั้น

---

# ขั้นตอนติดตั้ง

## ขั้นที่ 1 — เตรียม GitHub repo

1. สร้าง repo ใหม่ชื่อ `cmu-lost-found-final` แบบ **Public**
   (ต้อง public เพราะ GitHub Pages และ GitHub Actions ฟรีไม่จำกัดสำหรับ repo สาธารณะ)
2. อัปโหลดไฟล์ทั้งหมดตามโครงสร้างด้านบน
   ระวัง: โฟลเดอร์ `.github` ขึ้นต้นด้วยจุด บางระบบซ่อนไว้ ถ้าลากวางไม่ได้ให้สร้างไฟล์ผ่านหน้าเว็บ GitHub
   (Add file → Create new file → พิมพ์ `.github/workflows/match-and-notify.yml` ในช่องชื่อไฟล์)
3. Settings → Pages → Source: **Deploy from a branch** → branch `main` → folder `/ (root)` → Save
4. รอ 1–2 นาที จะได้ URL:
   `https://jinjutasum88-beep.github.io/cmu-lost-found-final/`

## ขั้นที่ 2 — ตั้งค่า Firebase Authentication

1. **เปิดวิธีล็อกอิน** — Authentication → Sign-in method → Email/Password → Enable → Save
2. **อนุญาตโดเมน** — Authentication → Settings → Authorized domains →
   ต้องมี `jinjutasum88-beep.github.io` อยู่ในรายการ (ทำแล้ว ✓)
3. **บังคับกฎรหัสผ่านฝั่งเซิร์ฟเวอร์** — Authentication → Settings → Password policy →
   Enable → ตั้ง Minimum length `8` และติ๊กให้ต้องมีตัวพิมพ์เล็ก ตัวพิมพ์ใหญ่ ตัวเลข และอักขระพิเศษ
   *ข้อนี้สำคัญ* เพราะกฎในหน้าเว็บเป็นแค่ฝั่งผู้ใช้ ใครยิง API ตรงๆ จะข้ามได้

## ขั้นที่ 3 — วาง Security Rules

Firestore Database → แท็บ **Rules** → ลบของเดิมทั้งหมด → วางเนื้อหาจาก `firestore.rules` → **Publish**

ถ้าไม่ทำข้อนี้ จะสมัครสมาชิกหรือลงประกาศไม่ได้เลย เพราะฐานข้อมูลเป็น production mode ที่ล็อกทุกอย่างไว้

> ไม่ต้องสร้าง composite index ใดๆ ทุกคำสั่งค้นหาในระบบนี้ใช้แค่เงื่อนไขความเท่ากัน
> ซึ่ง Firestore จัดการด้วย index อัตโนมัติอยู่แล้ว

## ขั้นที่ 4 — สร้าง Service Account key

สคริปต์ฝั่งเซิร์ฟเวอร์ต้องใช้กุญแจนี้เพื่อเข้าถึงฐานข้อมูลแบบข้าม security rules

1. Firebase Console → ⚙️ (มุมซ้ายบน) → **Project settings** → แท็บ **Service accounts**
2. กด **Generate new private key** → Generate key → ไฟล์ `.json` จะดาวน์โหลดลงเครื่อง
3. เปิดไฟล์ด้วย Text Editor แล้ว **คัดลอกเนื้อหาทั้งหมด** (ตั้งแต่ `{` ถึง `}`) เก็บไว้ใช้ในขั้นที่ 6

> ⚠️ ห้ามอัปไฟล์นี้ขึ้น repo เด็ดขาด ใครได้ไฟล์นี้ไปจะเข้าถึงฐานข้อมูลทั้งหมดได้
> ถ้าเผลอ push ขึ้นไปแล้ว ให้กลับไปที่หน้าเดิมแล้วกด Delete key ทิ้ง แล้วสร้างใหม่ทันที

## ขั้นที่ 5 — สมัคร Brevo เพื่อส่งอีเมล

เลือก Brevo เพราะฟรี 300 ฉบับ/วัน ไม่ต้องมีโดเมนของตัวเอง และไม่ต้องผูกบัตร

1. สมัครที่ `https://www.brevo.com` (สมัครด้วยอีเมลอะไรก็ได้ที่เข้าถึงได้จริง)
2. **ยืนยันอีเมลผู้ส่ง** — Senders, Domains & Dedicated IPs → Senders → Add a sender
   ใส่ชื่อ `CMU LOST&FOUND` และอีเมลของคุณ → Brevo จะส่งลิงก์ยืนยันไปที่อีเมลนั้น กดยืนยันให้เรียบร้อย
   (อีเมลนี้จะเป็นผู้ส่งของทุกฉบับที่ระบบส่งออกไป)
3. **สร้าง API key** — เมนูบัญชี (มุมขวาบน) → SMTP & API → แท็บ API keys → Generate a new API key
   ตั้งชื่อเช่น `cmu-lost-found` → **คัดลอกค่าเก็บไว้ทันที** เพราะจะแสดงครั้งเดียว

> ทางเลือกอื่นถ้าไม่อยากสมัคร Brevo: ใช้ Gmail + App Password ก็ได้ แต่ต้องแก้ฟังก์ชัน
> `sendEmail` ใน `scripts/match-and-notify.mjs` ให้ใช้ nodemailer แทน และ Gmail จำกัด 500 ฉบับ/วัน

## ขั้นที่ 5.5 — ขอ Gemini API key (สำหรับชั้น "เข้าใจความหมาย")

ฟรี ไม่ต้องผูกบัตรเครดิต ใช้บัญชี Google เดิมได้เลย

1. เข้า `https://aistudio.google.com/apikey`
2. ล็อกอินด้วยบัญชี Google → กด **Create API key**
3. เลือกโปรเจกต์ (จะเลือก `cmu-lost-found-final` ที่มีอยู่แล้วก็ได้) → Create
4. คัดลอกค่า key เก็บไว้ใช้ในขั้นถัดไป

> ข้ามขั้นนี้ได้ ระบบจะยังจับคู่ได้ตามปกติด้วย n-gram + TF-IDF
> เพียงแต่จะจับคู่คำที่เขียนต่างกันแต่หมายถึงของเดียวกันได้ไม่ดีเท่า

## ขั้นที่ 6 — ใส่ GitHub Secrets

ไปที่ repo → Settings → Secrets and variables → **Actions** → New repository secret
สร้างทั้งสามอันนี้:

| ชื่อ Secret | ค่าที่ใส่ |
|---|---|
| `FIREBASE_SERVICE_ACCOUNT` | เนื้อหา JSON ทั้งไฟล์จากขั้นที่ 4 (วางทั้งก้อน รวมวงเล็บปีกกา) |
| `BREVO_API_KEY` | API key จากขั้นที่ 5 |
| `SENDER_EMAIL` | อีเมลผู้ส่งที่ยืนยันกับ Brevo แล้ว |
| `GEMINI_API_KEY` | API key จากขั้นที่ 5.5 (ไม่ใส่ก็ได้ ระบบจะทำงานต่อโดยไม่ใช้ embedding) |

## ขั้นที่ 7 — เปิดและทดสอบ GitHub Actions

1. ไปที่แท็บ **Actions** ของ repo ถ้ามีปุ่มสีเขียวให้กด "I understand my workflows, go ahead and enable them"
2. เลือก workflow **"จับคู่ประกาศและส่งอีเมลแจ้งเตือน"** ที่แถบซ้าย
3. กด **Run workflow** → Run workflow (สั่งรันเองทันที ไม่ต้องรอครบ 5 นาที)
4. กดเข้าไปดู log ควรเห็นข้อความว่า `ไม่มีประกาศใหม่ที่ต้องจับคู่` แปลว่าเชื่อมต่อฐานข้อมูลสำเร็จ

ถ้าขึ้น error ให้ดูหัวข้อ **แก้ปัญหาที่พบบ่อย** ท้ายเอกสาร

## ขั้นที่ 8 — ตั้งตัวเองเป็นแอดมิน

1. เข้าเว็บ สมัครสมาชิกด้วยอีเมล @cmu.ac.th ของตัวเอง แล้วยืนยันอีเมลให้เรียบร้อย
2. Firebase Console → Firestore Database → คอลเลกชัน `users` → เอกสารของตัวเอง
3. แก้ฟิลด์ `role` จาก `user` เป็น `admin` → Update
4. รีเฟรชเว็บ จะเห็นปุ่ม "แอดมิน" บนแถบด้านบน

---

## ทดสอบระบบทั้งวงจร (ควรทำก่อนเปิดให้คนอื่นใช้)

ต้องใช้ **สองบัญชี** เพราะระบบไม่จับคู่ประกาศของคนเดียวกัน
ใช้อีเมล @cmu.ac.th สองอันของตัวเองก็ได้ (หรือใช้ท้ายอีเมลแบบ `ชื่อ+test@cmu.ac.th` ถ้าระบบเมลรองรับ)

1. **บัญชี A** ลงประกาศของหาย:
   หมวด `กระเป๋า` · สี `ดำ` · สถานที่ `คณะวิศวกรรมศาสตร์`
   รายละเอียด: `กระเป๋าสะพายสีดำยี่ห้อ Anello มีสติกเกอร์ลายแมวติดหน้ากระเป๋า`
2. **บัญชี B** ลงประกาศพบของ:
   หมวด `กระเป๋า` · สี `ดำ` · สถานที่ `คณะวิศวกรรมศาสตร์`
   รายละเอียด: `เจอกระเป๋าอนิลโล่สีดำ มีสติ๊กเกอร์รูปแมวติดอยู่ด้านหน้า`
3. ไปที่แท็บ Actions กด **Run workflow** (หรือรอไม่เกิน 5–15 นาที)
4. ทั้งสองบัญชีควรได้รับอีเมล และในเว็บจะขึ้นการแจ้งเตือน — คู่นี้ควรได้คะแนนราว **79%**
5. บัญชี A กด "✓ ใช่ นี่คือของฉัน"
6. รัน workflow อีกรอบ → ทั้งสองฝ่ายจะได้อีเมลที่มีข้อมูลติดต่อของกันและกัน
   และในเว็บจะเห็นอีเมลอีกฝ่ายในแท็บ "รายการที่จับคู่"

สังเกตว่าประกาศทั้งสองสะกดยี่ห้อคนละแบบ (`Anello` กับ `อนิลโล่`) และใช้คำต่างกันเกือบทั้งประโยค
แต่ระบบยังจับคู่ได้ — นี่คือผลของ NLP ไม่ใช่การเทียบข้อความตรงๆ

---

## เบื้องหลัง: NLP ใน `matching.js`

ภาษาไทยเขียนติดกันไม่มีเว้นวรรคระหว่างคำ วิธีตัดคำแบบภาษาอังกฤษจึงใช้ไม่ได้
ระบบจึงวิเคราะห์คำอธิบายด้วย **สองชั้นที่เก่งคนละแบบ** แล้วรวมคะแนนกัน

### ชั้นที่ 1 — Lexical (ดูที่ตัวอักษร)

| เทคนิค | ทำอะไร | สัดส่วน |
|---|---|---|
| Synonym normalization | ยุบคำสะกดต่างกันให้เป็นคำเดียว (`ไอโฟน`/`iPhone` → `iphone`, `อนิลโล่`/`Anello` → `anello`) | ขั้นเตรียมข้อมูล |
| Character trigram + Dice coefficient | ตัดข้อความเป็นชิ้นละ 3 ตัวอักษร วัดการซ้อนทับ — ทนคำสะกดผิด ใช้กับไทยได้โดยไม่ต้องมีตัวตัดคำ | 55% ของชั้นนี้ |
| TF-IDF cosine similarity | ให้น้ำหนักคำหายากมากกว่าคำทั่วไป (`สติกเกอร์ลายแมว` สำคัญกว่า `สีดำ`) | 45% ของชั้นนี้ |

เก่งเรื่อง: ยี่ห้อ เลขรุ่น คำเฉพาะ คำสะกดผิด
ไม่เก่งเรื่อง: คำที่เขียนคนละแบบแต่หมายถึงของเดียวกัน

### ชั้นที่ 2 — Semantic (ดูที่ความหมาย)

ใช้ **Gemini Embedding API** แปลงคำอธิบายเป็นเวกเตอร์ตัวเลข 768 มิติ
แล้ววัดมุมระหว่างเวกเตอร์ด้วย **cosine similarity**
ข้อความที่ความหมายใกล้กันจะได้เวกเตอร์ที่ชี้ไปทางเดียวกัน แม้ไม่มีตัวอักษรร่วมกันเลย

ตัวอย่างที่ชั้นที่ 1 ทำไม่ได้แต่ชั้นที่ 2 ทำได้: `เป้` กับ `กระเป๋าสะพาย`

**การประหยัดโควตา:** เรียก API เฉพาะคู่ที่ผ่านด่านหมวดหมู่+สีแล้วเท่านั้น
และเก็บเวกเตอร์ที่คำนวณแล้วไว้ในคอลเลกชัน `embeddings` เพื่อไม่ต้องคำนวณซ้ำ

**ถ้า API ล่มหรือโควตาหมด** ระบบจะถอยไปใช้ชั้นที่ 1 อย่างเดียวโดยอัตโนมัติ
การจับคู่ยังทำงานต่อได้ ไม่หยุดทั้งระบบ

### การรวมคะแนน

```
คะแนนคำอธิบาย = 50% × ชั้น Lexical + 50% × ชั้น Semantic
                 (ถ้าไม่มี embedding ใช้ชั้น Lexical 100%)

คะแนนรวม = สี 20% + คะแนนคำอธิบาย 55% + สถานที่ 15% + ความใกล้ของวันที่ 10%
```

โดยมี **หมวดหมู่ต้องตรงกัน** และ **สีต้องตรงกัน** เป็นด่านบังคับก่อน (ไม่ผ่านตัดทิ้งทันที)
เกณฑ์แจ้งเตือน **70%** สูงสุด **3 อันดับ** ต่อประกาศ
แก้ได้ที่ `MATCH_THRESHOLD` และ `MAX_MATCHES` ใน `matching.js`

เอกสารคู่จับเก็บคะแนนแยกทั้งสองชั้นไว้ในฟิลด์ `lexicalScore` และ `semanticScore`
เผื่อต้องการนำไปวิเคราะห์ว่าชั้นไหนช่วยได้มากกว่ากันตอนเขียนรายงาน

**ข้อสังเกตที่เจอตอนพัฒนา:** ตอนแรกใช้ TF-IDF กับทุกกรณี แต่พบว่าถ้าในระบบมีประกาศน้อยกว่า 10 ใบ
ค่า IDF จะเพี้ยน (คำที่โผล่ใน 2 จาก 2 ใบถูกลดน้ำหนักทั้งที่เป็นคำสำคัญ) ทำให้คู่ที่ตรงกันจริงได้ 67%
ตกเกณฑ์ จึงเพิ่มเงื่อนไขว่าถ้าคลังข้อมูลเล็กกว่า 10 ใบให้ข้าม TF-IDF คะแนนขึ้นเป็น 79% ผ่านเกณฑ์พอดี

## อีเมลยืนยันช้าและตกไปอยู่ใน Junk — สาเหตุและวิธีแก้

**ทำไมถึงตกไป Junk:** อีเมลยืนยันของ Firebase ส่งจาก
`noreply@cmu-lost-found-final.firebaseapp.com` ซึ่งเป็นโดเมนที่เราไม่ได้เป็นเจ้าของ
จึงไม่มี SPF/DKIM ที่ผูกกับโดเมนของโครงการ ผู้ให้บริการอีเมลเลยจัดเป็นความเสี่ยงไว้ก่อน
เป็นพฤติกรรมปกติของ Firebase ไม่ใช่ความผิดพลาดของระบบ

**ทำไมถึงช้า:** อีเมลของ Firebase ปกติใช้เวลา 1–5 นาที และจะช้าลงอีกถ้าส่งซ้ำหลายครั้งติดกัน
เพราะระบบจำกัดอัตราการส่ง

**สิ่งที่ทำได้ทันที (ทำแล้วในโค้ด):**
- หน้ารอยืนยันมีคำอธิบายให้ผู้ใช้ไปดูใน Spam/Junk และบอกชื่อผู้ส่งไว้ชัดเจน
- ปุ่มส่งซ้ำมีเวลาหน่วง 30 วินาที กันการกดรัวจนถูกจำกัดอัตรา

**สิ่งที่ควรทำเพิ่มใน Console (ช่วยได้จริง ใช้เวลา 2 นาที):**
Authentication → Templates → Email address verification → ✏️ Edit
- แก้ **From name** เป็น `CMU LOST&FOUND` แทนค่าเริ่มต้น
- ใส่ **Reply-to** เป็นอีเมล @cmu.ac.th ของคุณ
- แก้หัวข้อและเนื้อหาให้เป็นภาษาไทย ระบุชื่อโครงการชัดเจน
อีเมลที่มีชื่อผู้ส่งและเนื้อหาที่ดูเป็นทางการจะถูกจัดเป็น Spam น้อยลงอย่างเห็นได้ชัด

**วิธีแก้ให้หายขาด (ต้องมีโดเมนของตัวเอง):**
ถ้ามีโดเมน เช่น `cmulostfound.com` สามารถตั้ง custom domain ใน Firebase Auth
แล้วเพิ่ม SPF/DKIM ได้ อีเมลจะเข้ากล่องหลักเกือบ 100%
ทางเลือกที่ไม่ต้องซื้อโดเมนคือให้เซิร์ฟเวอร์สร้างลิงก์ยืนยันเองด้วย
`generateEmailVerificationLink()` แล้วส่งผ่าน Brevo เหมือนอีเมลแจ้งเตือนจับคู่
แต่ต้องรอรอบ cron ซึ่งอาจช้ากว่า Firebase ส่งเอง จึงยังไม่ทำในเวอร์ชันนี้

**สิ่งที่ควรบอกผู้ใช้ตอนเปิดใช้จริง:** ให้กด "ไม่ใช่จดหมายขยะ" ครั้งแรกที่เจอ
ครั้งต่อไปอีเมลจากระบบจะเข้ากล่องหลักเอง

---

## ระบบสองภาษา (`i18n.js`)

รองรับไทยและอังกฤษ สลับได้จากปุ่มมุมขวาบนทุกหน้า และตั้งค่าถาวรได้ในหน้าโปรไฟล์

- ครั้งแรกที่เข้าเว็บ ระบบเดาภาษาจากการตั้งค่าเบราว์เซอร์ ถ้าเป็นคนไทยจะได้ภาษาไทย
- ภาษาที่เลือกจำไว้ในเบราว์เซอร์ และบันทึกลงโปรไฟล์ด้วย เปลี่ยนเครื่องก็ยังได้ภาษาเดิม
- **ค่าที่บันทึกลงฐานข้อมูล (หมวดหมู่ สี สถานที่) เป็นภาษาไทยเสมอ** ไม่ว่าผู้ใช้จะเลือกภาษาอะไร
  เพราะระบบจับคู่ต้องเทียบกับค่าเดียวกัน คำแปลอังกฤษใช้แสดงผลอย่างเดียว

วิธีเพิ่มข้อความใหม่: เพิ่มคีย์ในตาราง `DICT` ทั้ง `th` และ `en`
แล้วใน HTML ใช้ `data-i18n="คีย์"` หรือใน JS ใช้ `t('คีย์')`

---

## การวัดความแม่นยำ (Precision / Recall)

```bash
npm run evaluate
```

**ทำไมต้องมีชุดนี้แยกจากข้อมูลผู้ใช้จริง:** ตัวเลขในแดชบอร์ดคำนวณจาก
"คู่ที่ผู้ใช้กดยืนยันว่าใช่ ÷ คู่ที่ผู้ใช้ตัดสินทั้งหมด" ซึ่งเป็น **precision** เท่านั้น
ระบบไม่มีทางรู้ว่ามีคู่ที่ตรงกันจริงกี่คู่ที่พลาดไป (**recall**)
เพราะคู่ที่ได้คะแนนต่ำกว่า 70% ไม่เคยถูกแสดงให้ใครเห็นเลย
`evaluation-dataset.mjs` แก้ปัญหานี้ด้วยการติดฉลากคำตอบที่ถูกต้องไว้ล่วงหน้า 20 คู่
(ตรงกันจริง 10 ไม่ตรงกัน 10) จึงวัดได้ครบทั้งสองด้าน

**ผลล่าสุด (ยังไม่เปิดใช้ embedding):**

| ตัวชี้วัด | ค่า | ความหมาย |
|---|---|---|
| Precision | 100% | คู่ที่ระบบเสนอ ถูกต้องทั้งหมด ไม่มีการรบกวนผู้ใช้โดยเปล่าประโยชน์ |
| Recall | 80% | คู่ที่ตรงกันจริง ระบบจับได้ 8 จาก 10 |
| F1 | 88.9% | |
| Accuracy | 90% | |

**Error analysis — คู่ที่ระบบพลาด 2 คู่:**

| กรณี | คะแนนที่ได้ | สาเหตุ |
|---|---|---|
| โทรศัพท์ รอยร้าวมุมจอ | 63% | ฝั่งหนึ่งเขียน "รอยร้าว" อีกฝั่งเขียน "รอยแตก" คนละคำแต่หมายถึงสิ่งเดียวกัน |
| กุญแจ พวงกุญแจตุ๊กตาหมา | 64% | ฝั่งหนึ่งเขียน "ตุ๊กตาหมา" อีกฝั่งเขียน "รูปสุนัข" |

ทั้งสองกรณีเป็น**คำต่างที่ความหมายเหมือนกัน** ซึ่งเป็นจุดอ่อนของชั้นเทียบตัวอักษรโดยตรง
และเป็นเหตุผลที่ระบบต้องมีชั้น embedding — เมื่อตั้ง `GEMINI_API_KEY` แล้ว
คู่เหล่านี้ควรได้คะแนนสูงขึ้นจนผ่านเกณฑ์ ตรงนี้เป็นข้อมูลที่ควรวัดซ้ำแล้วเทียบใส่ในรายงาน

การประเมินนี้รันอัตโนมัติในชุดทดสอบด้วย ถ้าใครแก้โค้ดจนความแม่นยำตกต่ำกว่า 70% เทสต์จะแดงทันที

---

## ชุดทดสอบ (Unit Tests)

```bash
npm test              # รันชุดทดสอบทั้งหมด
npm run test:coverage # รันพร้อมวัด Coverage
```

ใช้ `node:test` ที่ติดมากับ Node.js เอง ไม่ต้องติดตั้ง framework เพิ่ม
และตั้งให้รันอัตโนมัติทุกครั้งที่ push ผ่าน `.github/workflows/tests.yml`

**ผลล่าสุด: 75 tests ผ่านทั้งหมด**

| ไฟล์ | Line | Branch | Function |
|---|---|---|---|
| `matching.js` | 100% | 88% | 100% |
| `password-policy.js` | 100% | 93% | 100% |
| `firebase-config.js` | 100% | 100% | 100% |
| **รวม** | **100%** | **94%** | **99%** |

ชุดทดสอบครอบคลุม Test Case ที่ประกาศไว้ตอนนำเสนอกลางภาค:

| Test Case ที่พรีไว้ | ทดสอบใน |
|---|---|
| TC-01 สมัครสมาชิกด้วยอีเมล @cmu.ac.th | `auth.test.mjs` → isAllowedEmail |
| TC-02 สมัครด้วยอีเมลซ้ำ | `auth.test.mjs` → thaiError |
| TC-03 อีเมลตัวพิมพ์ใหญ่ผสม | `auth.test.mjs` → isAllowedEmail |
| TC-06 โพสต์ที่มีแต่อีโมจิ | `matching.test.mjs` → hasReadableText |
| TC-07 จับคู่ปกติ | `matching.test.mjs` → findMatches |
| TC-08 หมวดหมู่/สีต่างกัน | `matching.test.mjs` → passesHardFilter |
| TC-09 บัญชีเดียวกันโพสต์ทั้งสองฝั่ง | กรองในสคริปต์เซิร์ฟเวอร์ + ทดสอบ findMatches ไม่จับคู่กับตัวเอง |

(TC-04 ลงประกาศครบถ้วน และ TC-05 อัปโหลดไฟล์ผิดชนิด เป็นการทดสอบระดับหน้าจอ
ทำเป็น manual test พร้อมบันทึกผลตอนทำ user testing)

**บั๊กที่ชุดทดสอบนี้ล็อกไว้ไม่ให้กลับมาอีก:** กรณี TF-IDF เพี้ยนเมื่อคลังข้อมูลเล็ก
มีเทสต์เฉพาะที่ชื่อ "คลังเล็กต้องไม่ทำให้คู่ที่ตรงกันจริงตกเกณฑ์" ถ้าใครไปแก้โค้ดจนบั๊กกลับมา เทสต์จะแดงทันที

## นโยบายรหัสผ่าน (`password-policy.js`)

**กฎแบบดั้งเดิม** — ยาว 8–64 ตัว ต้องมีตัวพิมพ์เล็ก ตัวพิมพ์ใหญ่ ตัวเลข และอักขระพิเศษ ครบสี่อย่าง

**กฎแบบใหม่ตามมาตรฐาน NIST SP 800-63B** — เพราะ `Password1!` ผ่านกฎข้างบนทุกข้อ
แต่เป็นหนึ่งในรหัสที่ถูกเดาบ่อยที่สุดในโลก จึงเพิ่มการดัก:
- รหัสยอดนิยม รวมถึงรูปแบบที่เติมเลข/สัญลักษณ์ต่อท้าย
- รหัสที่มีชื่อหรืออีเมลของผู้ใช้เองอยู่ข้างใน
- ลำดับที่คาดเดาได้ `1234` `abcd` `qwerty` (รวมแบบย้อนกลับ)
- ตัวอักษรเดิมซ้ำติดกันเกิน 3 ตัว

หน้าสมัครแสดงเงื่อนไขทั้ง 11 ข้อพร้อม ✓/✕ แบบเรียลไทม์ มีแถบวัดความแข็งแรง 5 ระดับ
ปุ่มแสดง/ซ่อนรหัสผ่าน และปุ่มสุ่มรหัสผ่านที่ผ่านทุกกฎ

**สิ่งที่จงใจไม่ทำ:** ไม่บังคับเปลี่ยนรหัสผ่านตามรอบเวลา เพราะ NIST เลิกแนะนำแล้ว —
การบังคับเปลี่ยนทำให้ผู้ใช้ตั้งรหัสวนซ้ำแบบ `Password1!` → `Password2!` ซึ่งอ่อนกว่าเดิม

---

## ความปลอดภัยและความเป็นส่วนตัว

| เรื่อง | วิธีจัดการ |
|---|---|
| ประกาศของคนอื่น | ผู้ใช้อ่านไม่ได้เลย security rules กรองที่ระดับฐานข้อมูล ไม่ใช่แค่ซ่อนใน UI |
| รายละเอียดของคู่ที่จับได้ | เซิร์ฟเวอร์ฝัง "สำเนาย่อ" ไว้ในเอกสารคู่จับ ผู้ใช้เห็นเฉพาะคู่ที่ตัวเองเกี่ยวข้อง |
| อีเมลของอีกฝ่าย | ไม่มีอยู่ในเอกสารเลยจนกว่าจะกดยืนยัน เซิร์ฟเวอร์ค่อยเขียนเพิ่มทีหลัง |
| การแก้ข้อมูล | เจ้าของแก้ได้เฉพาะฟิลด์ `status` ของประกาศตัวเอง แก้ฟิลด์อื่นไม่ได้ |
| ผลการจับคู่ | ผู้ใช้เขียนไม่ได้เลย เขียนได้เฉพาะเซิร์ฟเวอร์ แก้ได้แค่ `matchStatus` ของตัวเอง |
| รหัสผ่าน | Firebase แฮชด้วย scrypt ไม่เก็บเป็นข้อความธรรมดา และจำกัดจำนวนครั้งที่ล็อกอินผิด |
| ค่าใน `firebase-config.js` | เปิดเผยได้ปกติ ไม่ใช่ความลับ ความปลอดภัยจริงอยู่ที่ security rules |

---

## ค่าใช้จ่ายและโควตา (ทั้งหมดอยู่ในแพ็กเกจฟรี)

| บริการ | โควตาฟรี | การใช้จริงของระบบนี้ |
|---|---|---|
| Firebase Spark | อ่าน 50,000 ครั้ง/วัน · เขียน 20,000 ครั้ง/วัน | สคริปต์อ่านประกาศทั้งหมดเฉพาะตอนที่มีประกาศใหม่เท่านั้น รอบที่ว่างเปล่าใช้แค่ 2 คำสั่งค้นหา |
| GitHub Actions | ไม่จำกัดสำหรับ repo สาธารณะ | 288 รอบ/วัน รอบละไม่ถึง 1 นาที |
| GitHub Pages | ไม่จำกัด | โฮสต์ไฟล์ static |
| Brevo | 300 อีเมล/วัน | 2 ฉบับต่อการจับคู่ 1 คู่ |
| Gemini Embedding | มีโควตาฟรีต่อนาที/ต่อวัน | เรียกเฉพาะคู่ที่ผ่านด่านแรก และเก็บผลไว้ใช้ซ้ำ |

---

## หน้าตั้งค่าโปรไฟล์

ผู้ใช้แก้ไขได้เอง: ชื่อที่แสดง · เบอร์ติดต่อ · คณะ · เปิด/ปิดการแจ้งเตือนทางอีเมล · ภาษา
พร้อมปุ่มขอลิงก์เปลี่ยนรหัสผ่าน และปุ่มลบบัญชีถาวร

**ข้อควรรู้:** อีเมลเปลี่ยนไม่ได้เพราะผูกกับการยืนยันตัวตน
และการปิดแจ้งเตือนทางอีเมลมีผลจริงกับฝั่งเซิร์ฟเวอร์ — สคริปต์จะอ่านค่า `notifyEmail`
ก่อนส่งทุกฉบับ ถ้าปิดไว้จะข้ามการส่งและบันทึกไว้ใน log

## การป้องกันการใช้งานผิดวัตถุประสงค์

| มาตรการ | รายละเอียด |
|---|---|
| จำกัดจำนวนโพสต์ | วันละไม่เกิน 5 รายการต่อบัญชี และมีประกาศที่เปิดอยู่พร้อมกันได้ไม่เกิน 15 รายการ |
| รายงานเนื้อหา | ผู้ใช้กดรายงานประกาศที่เจอในหน้าจับคู่ได้ เข้าคิวให้แอดมินตรวจ |
| เครื่องมือแอดมิน | ซ่อนหรือลบประกาศได้จากแดชบอร์ด พร้อมรายการรายงานที่รอตรวจ |
| ประกาศคงอยู่จนกว่าจะปิดเอง | ไม่มีการปิดอัตโนมัติตามเวลา ของหายอาจถูกพบหลังผ่านไปหลายเดือน การปิดประกาศทิ้งจะทำให้ระบบไม่จับคู่ให้ในวันที่มีคนเก็บได้จริง |
| เก็บกวาดข้อมูล | ผลจับคู่และเวกเตอร์ที่ชี้ไปยังประกาศที่ถูกลบ จะถูกลบทิ้งในรอบ cron |

**ข้อจำกัดที่ต้องยอมรับ:** การจำกัดจำนวนโพสต์บังคับจากฝั่งเว็บ ไม่ใช่ security rules
เพราะ Firestore rules นับจำนวนเอกสารข้ามคอลเลกชันไม่ได้ ผู้ที่เขียนสคริปต์ยิง API ตรงๆ
ยังข้ามได้ในทางทฤษฎี แต่จะถูกจับได้จากแดชบอร์ดแอดมินและลบทีหลัง
ถ้าจะปิดช่องนี้จริงต้องใช้ Cloud Functions ซึ่งต้องผูกบัตรเครดิต

---

## การคุ้มครองข้อมูลส่วนบุคคล — ต้องยินยอมทั้งสองฝ่าย

การแลกข้อมูลติดต่อผ่านสามขั้น ไม่ใช่ฝ่ายเดียวตัดสิน:

```
waiting_for_user   ระบบเสนอคู่ให้เจ้าของของหายดู
      ↓ เจ้าของของหายกด "ใช่ นี่คือของฉัน"
awaiting_finder    ผู้ที่เก็บของได้ถูกถามว่ายินยอมเปิดเผยอีเมลหรือไม่
      ↓ ผู้เก็บของได้กด "อนุญาต"
accepted           เซิร์ฟเวอร์จึงเขียนอีเมลลงเอกสารและส่งให้ทั้งสองฝ่าย
```

ถ้าผู้เก็บของได้กด "ไม่อนุญาต" สถานะเป็น `declined` และไม่มีการเปิดเผยอีเมลใดๆ เลย
ก่อนถึงขั้น `accepted` เอกสารคู่จับไม่มีอีเมลของใครอยู่เลย ไม่ใช่แค่ซ่อนใน UI

## ข้อจำกัดที่ควรรู้

- **รูปภาพเก็บเป็น base64 ในฐานข้อมูล** ไม่ได้ใช้ Firebase Storage เพราะโปรเจกต์ที่สร้างใหม่
  ต้องผูกบัตรเครดิตถึงจะใช้ Storage ได้ ระบบจึงย่อรูปเหลือด้านยาวสุด 800px จำกัดราว 700KB ต่อรูป
- **การจับคู่ไม่ได้เกิดขึ้นทันที** GitHub Actions cron ตั้งไว้ทุก 5 นาที แต่ GitHub มักหน่วงจริง 5–15 นาที
  ในช่วงที่คนใช้เยอะ ถ้าอยากได้ทันทีต้องย้ายไป Cloud Functions ซึ่งต้องผูกบัตร
- **ข้อมูลติดต่อเปิดเผยช้ากว่าการกดยืนยัน** เพราะรอรอบ cron ถัดไป หน้าเว็บจะขึ้นข้อความว่ากำลังดำเนินการ
- **ระบบไม่เปรียบเทียบรูปภาพ** วิเคราะห์เฉพาะข้อความ ผู้ใช้ต้องดูรูปเองก่อนกดยืนยัน
- **รูปเก็บสองขนาด** รูปย่อ (240px, ~50KB) ฝังในเอกสารประกาศเพื่อให้หน้ารายการเบา
  ส่วนรูปเต็ม (800px) แยกไว้ที่คอลเลกชัน `postImages` โหลดเฉพาะตอนเปิดดูรายละเอียด
  ถ้าไม่ทำแบบนี้ หน้ารายการที่มี 20 ประกาศจะต้องดาวน์โหลดข้อมูลราว 14MB ทุกครั้งที่เปิด
- **การจับคู่ข้ามภาษา** ระบบมีตารางคำพ้องอังกฤษ–ไทยสำหรับคำที่พบบ่อย
  (bag/กระเป๋า, black/ดำ ฯลฯ) แต่คำนอกตารางจะพึ่งชั้น embedding เป็นหลัก
  ถ้าไม่ได้ตั้ง `GEMINI_API_KEY` การจับคู่ข้ามภาษาจะอ่อนลงมาก
- **ประกาศไม่มีวันหมดอายุ** ปิดได้สองทางเท่านั้น คือเจ้าของกดว่าได้ของคืน
  หรือยืนยันการจับคู่สำเร็จ เคยพิจารณาให้ปิดอัตโนมัติเมื่อครบ 90 วันแต่ตัดออก
  เพราะจะทำให้ระบบไม่จับคู่ให้ในกรณีที่ของถูกพบหลังผ่านไปนาน ซึ่งขัดกับจุดประสงค์หลัก
  ถ้าในอนาคตข้อมูลเยอะจนช้า ควรแก้ด้วยการทำ index หรือแบ่งชุดข้อมูล ไม่ใช่การปิดประกาศทิ้ง
- **scheduled workflow ถูกปิดอัตโนมัติ** ถ้า repo ไม่มีการเคลื่อนไหวเกิน 60 วัน
  ช่วงปิดเทอมควรเข้าไปกด Run workflow เองเดือนละครั้ง

---

## แก้ปัญหาที่พบบ่อย

**สมัครสมาชิกแล้วขึ้น error `Missing or insufficient permissions`**
ยังไม่ได้วาง security rules → กลับไปทำขั้นที่ 3

**ลงประกาศไม่ได้ ขึ้น permission denied**
ตรวจว่า rules ที่วางเป็นเวอร์ชันล่าสุด (ต้องมีเงื่อนไข `processed == false` ในส่วน create)

**Actions รันแล้ว fail ที่ขั้น `รันตัวจับคู่และส่งอีเมล`**
- `Unexpected token in JSON` → วาง `FIREBASE_SERVICE_ACCOUNT` ไม่ครบ ต้องวางทั้งไฟล์รวมปีกกา
- `PERMISSION_DENIED` → Service Account key มาจากคนละโปรเจกต์กับที่ใช้อยู่
- `ไม่พบ FIREBASE_SERVICE_ACCOUNT` → ตั้งชื่อ secret ผิด ต้องตรงตัวพิมพ์ใหญ่-เล็ก

**Actions รันผ่าน แต่ไม่ได้รับอีเมล**
- ดู log ถ้าขึ้น `[ข้ามการส่งอีเมล]` แปลว่ายังไม่ได้ตั้ง `BREVO_API_KEY` หรือ `SENDER_EMAIL`
- ถ้าขึ้น `ส่งอีเมลไม่สำเร็จ: 400` มักเป็นเพราะยังไม่ได้ยืนยันอีเมลผู้ส่งกับ Brevo
- ตรวจกล่องจดหมายขยะ (Spam) ด้วย อีเมลอัตโนมัติมักตกไปที่นั่นในช่วงแรก

**ลงประกาศสองใบที่ควรตรงกัน แต่ระบบไม่จับคู่ให้**
- ระบบไม่จับคู่ประกาศของคนเดียวกัน ต้องใช้คนละบัญชี
- หมวดหมู่และสีต้องตรงกันเป๊ะ ถ้าใบหนึ่งเลือก `กระเป๋า` อีกใบพิมพ์เอง `กระเป๋าเป้` จะไม่ตรงกัน
- คะแนนอาจต่ำกว่า 70% ลองเขียนรายละเอียดให้มีคำเฉพาะเจาะจงร่วมกันมากขึ้น
- ดู log ของ Actions จะบอกว่าประกาศแต่ละใบพบคู่ที่เข้าเกณฑ์กี่รายการ

**log ขึ้น `[embedding] เรียก API ไม่สำเร็จ`**
- `403` หรือ `API key` → key ผิดหรือยังไม่ได้เปิดใช้ ตรวจที่ Google AI Studio
- `429` → โควตาต่อนาทีเต็ม ระบบจะข้ามไปใช้ n-gram รอบนี้ แล้วรอบถัดไปจะลองใหม่เอง
- ไม่ว่ากรณีไหน การจับคู่ยังทำงานต่อได้ ไม่ต้องรีบแก้

**Actions ไม่รันเองตามเวลา**
GitHub ปิด scheduled workflow อัตโนมัติถ้า repo ไม่มีการเคลื่อนไหวเกิน 60 วัน
ถ้าเกิดขึ้นให้เข้าไป push อะไรสักอย่างหรือกด Run workflow เองหนึ่งครั้ง
